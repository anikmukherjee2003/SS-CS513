# Problem 13: Waiting for STDIN Using `select()`

## Problem statement
Wait for input from standard input for 10 seconds using `select()`. Print whether input was available before the timeout.

## Key call
- `select()`

## Execution
Run the program and either type input within 10 seconds or wait for the timeout message.
