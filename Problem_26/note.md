# Problem 26: Executing `ls -Rl` with `exec` Calls

## Problem statement
Execute `ls -Rl` using `execl()`, `execlp()`, `execle()`, `execv()`, and `execvp()`.

## Key calls
- `execl()`
- `execlp()`
- `execle()`
- `execv()`
- `execvp()`

## Execution
Compile the caller and target programs as required, then run each variant from the appropriate directory.
