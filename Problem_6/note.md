# Problem 6: Input and Output Using System Calls

## Problem statement
Read input from standard input and display it on standard output using only `read()` and `write()` for data transfer.

## Key calls
- `read()`
- `write()`

## Execution
Compile and run the program, then type input followed by Enter. Standard input is descriptor `0` and standard output is descriptor `1`.
