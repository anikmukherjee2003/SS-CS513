# Problem 20: Printing Parent and Child Process IDs

## Problem statement
Call `fork()` and print the process IDs of the parent and child processes.

## Key calls
- `fork()`
- `getpid()`
- `getppid()`

## Execution
Compile and run the program. Both execution paths should report their process relationships.
