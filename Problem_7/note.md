# Problem 7: File Copying

## Problem statement
Copy the contents of `file1` into `file2`, emulating the behavior of the `cp file1 file2` command.

## Key calls
- `open()`
- `read()`
- `write()`
- `close()`

## Execution
Compile the program and provide source and destination paths as command-line arguments.
