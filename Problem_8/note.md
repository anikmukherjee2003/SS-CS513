# Problem 8: Read-Only File Reading

## Problem statement
Open a file in read-only mode, read it line by line, display each line, and close the file at end-of-file.

## Key calls
- `open()`
- `read()`
- `close()`

## Execution
Compile the program and pass the input filename as a command-line argument.
