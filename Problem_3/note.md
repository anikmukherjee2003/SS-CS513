# Problem 3: File Creation and Descriptor Printing

## Problem statement
Create a file with `creat()` and print the file descriptor returned by the system call.

## Key call
- `creat()`

## Execution
Compile and run the program. The returned descriptor can be compared with the standard descriptors `0`, `1`, and `2`.
