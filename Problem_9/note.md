# Problem 9: File Information Extraction

## Problem statement
Print a file's inode, hard-link count, UID, GID, size, block size, block count, and access, modification, and change times.

## Key call
- `stat()`

## Execution
Compile and run the program against the file whose metadata should be displayed.
